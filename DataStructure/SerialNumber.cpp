#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int Compare(string A, string B)
{
	int ASize = A.size();
	int BSize = B.size();

	int ASum = 0;
	int BSum = 0;

	//1.길이가 다르면 짧은 순서가 먼저
	if(ASize != BSize)
	{
		
	}

	//2.길이가 같다면 자리값의 합이 큰쪽이 먼저
	for(int i=0; i<ASize; i++)
	{
		
	}


}




//3.사전 순으로 비교 숫자 알파벳보다 더 작음


int main()
{
	int N;
	string Serial[50];
	cin >> N;
	for(int i=0; i<N; i++)
	{
		cin >> Serial[i];
	}



}